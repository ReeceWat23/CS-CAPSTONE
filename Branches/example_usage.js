
// Need a full crud for branches + the ability to remove agents from the branches

// What the data looks like 

/* Branch data
// agents = list of useers 
Branch_name string 
link = string 
logo = image ( bubble handles this well though )
owner_id = string | owner of the branch the user who will manage the branch as an admin

* colors must be in HEX format #{hex number} --- ensure this formatting 
primary_color string 
secondary_color string | 


refs referral list === however we can search and mearch a all agents to create a master list of referrals 
here we might have to add another search end point to handle fetching all the referrals in their list though actually scratch that initially we'll append all associated public refferals with an agent but whenever that agent leaves we can remove those referrals from the branches master list 





*/

